#ifndef MISFUNCIONES_H_INCLUDED
#define MISFUNCIONES_H_INCLUDED

/* Prototipo de la función "sumar" encargada de realizar la operación de suma entre dos parámetros. */
int sumar(int, int);

/* Prototipo de la función "restar" encargada de realizar la operación de resta entre dos parámetros. */
int restar(int, int);

/* Prototipo de la función "multiplicar" encargada de realizar la operación de multiplicación entre dos parámetros. */
int multiplicar(int, int);

/* Prototipo de la función "dividir" encargada de realizar la operación de división entre dos parámetros. */
int dividir(int, int);

/* Prototipo de la función "factorial" encargada de realizar la operación de descomposición factorial de un parámetro.  */
int factorial(int);

#endif // MISFUNCIONES_H_INCLUDED

